apiVersion: v1
kind: Service
metadata:
  name: webrtc-vue-service
  namespace: webrtc
spec:
  type: LoadBalancer
  selector:
    app: webrtc-vue
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: signaling
      port: 3001
      targetPort: 3001
